<div align="center">

[English](README.md) | [简体中文](README_zh-CN.md) | [繁體中文](README_zh-TW.md) | **日本語** | [한국어](README_ko.md)

<img src="docs/assets/logo.png" width="96" alt="LoongCode logo">

# LoongCode

**Claude Code と OpenAI Codex をデスクトップに詰め込んだ AI Agent IDE**

Claude Code CLI と OpenAI Codex CLI をモダンなデスクトップワークベンチに包み込みました —— マルチセッション対話、統合ターミナル、ファイルツリー、Git Review、コマンド / ファイルパレット、そして MCP・プラグイン・スキル・モデルプロバイダーのビジュアル管理まで、箱から出してすぐに使えます。

![version](https://img.shields.io/badge/version-0.9.5-6d5efc)
![platform](https://img.shields.io/badge/platform-Windows%20%7C%20macOS-444)
![built with](https://img.shields.io/badge/built%20with-Tauri%202%20%2B%20React%2019-2f6df6)

[![バージョン履歴](https://img.shields.io/badge/%E3%83%90%E3%83%BC%E3%82%B8%E3%83%A7%E3%83%B3%E5%B1%A5%E6%AD%B4-%E3%82%AA%E3%83%B3%E3%83%A9%E3%82%A4%E3%83%B3%E3%81%A7%E8%A6%8B%E3%82%8B-6d5efc)](https://release.loongcode.cc)
[![📖 使用ガイド](https://img.shields.io/badge/%F0%9F%93%96%20%E4%BD%BF%E7%94%A8%E3%82%AC%E3%82%A4%E3%83%89-%E3%82%AF%E3%83%AA%E3%83%83%E3%82%AF%E3%81%97%E3%81%A6%E8%A1%A8%E7%A4%BA-0ea5e9?style=flat&labelColor=0f172a)](https://docs.loongcode.cc)

</div>

> **[📖 完全な使用ガイドを見る →](https://docs.loongcode.cc)**　すべての機能、設定のコツ、ベストプラクティスを解説しています。


<div align="center">

### 🎬 プロモーション動画 · LoongCode が動く様子を見る



https://github.com/user-attachments/assets/f679fd9d-627c-4376-8af3-7ca907049eab



</div>

> 📦 **ここは LoongCode の公式リリースリポジトリです。** 各プラットフォーム向けインストーラーのダウンロードを提供するとともに、アプリの「自動更新」のダウンロード元（`latest.json`）としても機能します。ソースコードや開発ドキュメントが必要な場合は、プロジェクトのメインリポジトリをご覧ください。

> 📈 **バージョンタイムライン / 今後のロードマップ**：**<https://release.loongcode.cc>** —— 各バージョンで完成した機能のサマリーと、今後の計画（モバイルネイティブ App / Agent Team など）をタイムライン形式で紹介しています。

---

## LoongCode とは

**LoongCode** は **Tauri 2 + React 19** ベースのクロスプラットフォーム・デスクトップ **Agent IDE** で、その本質は **Claude Code、OpenAI Codex など複数の Agent CLI を束ねる統一グラフィカルシェル**です（今後も対応 CLI を継続的に追加していきます）。

これまでコマンドラインの中でしか使えなかった Agent プログラミング体験を、本物の IDE スタイルのインターフェースへ持ち込みました：

- 一つひとつの「タスク」が独立した AI セッションに対応し、**Claude Code**、**OpenAI Codex** など複数の CLI を必要に応じて切り替えられます（タスクごとに個別選択、モデル / 設定はそれぞれ独立して記憶されます）。
- アプリはローカルの `claude` または `codex` CLI をサブプロセスとして起動し、ストリーミング JSON プロトコルでリアルタイムに通信します。
- イベントストリームをリアルタイムに対話 UI としてレンダリングし、同じウィンドウの中にターミナル、ファイルツリー、Git Review、コマンド / ファイルパレット、各種設定を統合しています。

> 💡 こう考えると分かりやすいでしょう：**CLI がエンジン、LoongCode がコックピット** —— モデル呼び出し・コード実行・ファイル操作は CLI に任せ、マルチタスク管理・履歴復元・Git 連携などのワークフロー強化は LoongCode が担います。両者は疎結合で動作し、それぞれ独立してアップグレードできます。

> ⚠️ **重要：LoongCode 自身はいかなるモデル API も直接呼び出しません。** すべてのモデルとのやり取りは、LoongCode が起動する CLI サブプロセスが行い、API キーとモデル設定も CLI 側で管理されます。そのため利用を始める前に、ローカル環境に**必須依存関係**を準備しておく必要があります：インストールとログインが完了した **Claude Code CLI** と **Git** です。**OpenAI Codex** ファミリーのタスクも使いたい場合は、さらに**オプション依存関係**である **Codex CLI** をインストールすれば OK です。
>
> 👉 未インストールでもアプリを離れる必要はありません：LoongCode には**「依存関係管理」**パネルが内蔵されており、Claude Code CLI、Git、Codex CLI をワンクリックで直接インストールできます（下記「システム要件」と「🧩 依存関係とワンクリックインストール」を参照）。ログインはインストール後にご自身で行ってください。

---

## ✨ 主な機能

### セッションとタスク
- **マルチ CLI 対応（Claude / OpenAI Codex / OpenCode）** —— タスクは **Claude Code**、**OpenAI Codex**、**OpenCode** など複数の CLI ファミリー間を必要に応じて切り替えられます（タスク / 下書きごとに個別選択、設定はそれぞれ独立して記憶されます）。Codex と OpenCode のタスクも、Claude と足並みを揃えたフル体験が得られます：対話の**ストリーミング出力**、**インタラクティブな承認 + グレースフルな中断**、**履歴復元**、**サブエージェントのサブカード**、**セッションフォーク**、そしてスキル / プラグイン / MCP / モデルプロバイダーのビジュアル管理。内部では Codex は `codex app-server` 経由、OpenCode は `opencode serve` + SSE イベントストリームの持続接続で駆動され、タスク / ディレクトリごとに 1 プロセス・遅延起動・アイドル時回収という構成です。新たに加わった **OpenCode** は、**アプリ内モデルプロバイダーの再利用**（OpenAI 互換、config 経由で注入されるためキーの手動設定は不要）、**タスクごとの思考深度 / 権限レベル**、**worktree セッション移行**にも対応し、そのツールカードと Read 結果はいずれも Claude スタイルに統一されています。
- **マルチワークスペース / マルチタスク管理** —— 各タスクは独立した復元可能なセッションです。タスクリストは最近のアクティビティ順に並び、使用中のタスクは自動的に上へ浮かび上がります。タスクを選択すると、その所属グループ（ワークスペース / ピン留め / タイムラインの日付バケット）が自動的に展開されフォーカスされます。
- **分割対話（マルチペイン）** —— メインエリアでは対話を複数のペインに分割できます（左右 / 上下の再帰的タイリング、分割線のドラッグ、閉じて折りたたみ、**ペインのタイトルをドラッグしてドッキング / 入れ替え・並べ替え**）。各ペインは 1 つの完全な対話または新規下書きで、**ワークスペースをまたいで自由に混在**させられます。ペインを選択するとサイドバー / ファイル / ターミナル / Git が自動で追従し、レイアウトは記憶され、誤って閉じても `Ctrl+Shift+T` で取り消せます。
- **タスクのアーカイブ** —— アクティブでなくなったタスクは、手動（ドロップダウン / 右クリック）でも、最近のアクティビティ時刻に基づく自動でもアーカイブでき、独立した「アーカイブビュー」に収納してメインリストをすっきり保てます。自動アーカイブは設定でオン / オフとしきい値（時間 / 日 / 月）を調整でき、実行中 / ピン留めのタスクは保護されます。
- **新規タスクの下書き状態** —— 「新規」をクリックするとまず下書きに入り、ワークスペース / モデル / Git ブランチまたは worktree をインラインで選択できます。最初のメッセージを送信して初めてタスクが正式に作成されます（`Ctrl+N` でクイック作成、既定では前回使ったワークスペースが選択されます）。
- **履歴の正確な復元** —— CLI のネイティブなセッション記録（Claude の JSONL、Codex の rollout）にアプリ側の sidecar を組み合わせて対話を復元します。ユーザー入力内のファイル・コマンド・画像 chip のアイデンティティまで 1:1 で再現されます。
- **セッションフォーク / グレースフルな中断 / 再実行** —— 履歴の任意のノードから新しいセッションを fork できます。「停止」のクリックは ESC によるグレースフルな中断と同等で、生成済みの内容を保持したまま対話を続けられます。
- **過去のユーザーメッセージを編集**し、その時点から続行できます。
- **サブ Agent のサブ対話** —— サブ Agent の対話は独立した折りたたみカードにルーティングされます（ヘッダーカード / 作業レポート / デグレードストリームの 3 層による段階的開示）。メインスレッドは明瞭なまま干渉しません。
- **バックグラウンドタスクのアクティビティ表示と閲覧** —— `run_in_background` で起動したバックグラウンドコマンド、非同期サブ Agent、サービスプロセス、**動的ワークフロー**などは、全過程がリアルタイムで見え、「実行中」のまま固まることは決してありません：タスク行とタイトルバーでは **⚡N インジケーター**が件数を集約し、完了 / 失敗はハイライトドットで既読・未読を表現します（ポップアップも音も出しません）。**グループオーバーレイ**を開けばすべてのバックグラウンド項目を素早く確認でき（ワークフロー項目には専用アイコンと種別ヒント付き）、バックグラウンドタスクを起動したツールカードの下部には**バックグラウンドステータスバー**が追加され（バックグラウンドサブ Agent はその場で展開してサブ対話を確認でき、ネストされたサブ Agent のリアルタイムレンダリングも含みます）、コマンド / サービス系は右側に**閉じられるリアルタイムログビュー**を開くこともできます。
- **Claude 動的ワークフローの可視化（ノンブロッキング）** —— Claude Code の動的ワークフロー（ユーザー / プロジェクトの `.claude/workflows/*.js`）を発見してトリガーし、実行進捗をリッチな進捗カードとしてレンダリングします：フェーズごとのグループ化（実行中は自動展開 / アイドル時は自動折りたたみ）、サブエージェントへのドリルダウンで transcript を確認、使用量と結果が一目瞭然、アプリを再起動してもディスクからカードを再構築できます。ワークフローは**全過程バックグラウンドでノンブロッキング実行**されます：実行中も入力ボックスはロックされず対話を続けられ、バックグラウンドタスク体系に統合されます（⚡ インジケーター / 未読ドット / サイドバーのタイマー / バックグラウンドアクティビティオーバーレイが同期してカバー）。長時間実行のワークフローがアイドル回収で誤って終了させられることはなく、プロセスの異常終了や手動終了も正直に失敗としてマークされます。
- **メッセージの定時実行** —— タスクに予約送信を設定できます：指定時刻 / 間隔 / 毎日 / Cron の 4 つのモードに対応し、依存関係不要の日時ピッカーを内蔵、下書き状態のタスクでも予約できます。予約状態は紫色のインジケーターで表示され、グローバル概要でまとめて確認・管理できます。
- **モバイル対応（WeChat ClawBot / Lark）** —— WeChat 公式の ClawBot または Lark ボットを連携すると、スマートフォンからリモートでタスクを新規作成 / 駆動し、AI の返信を受け取れます。インタラクティブな質問（AskUserQuestion）にも数字を返信して回答できます。Lark チャンネルは**ネイティブ Rust の持続接続**として実装されています：QR コードスキャンでアプリ連携を確立し、プライベートチャットは全転送 / グループでは @ メンションでボットをトリガー、接続ヘルスインジケーター（緑 / 黄 / グレー）を備え、ネットワークが不安定でも自動再接続し資格情報を失いません。
- **PC リモートブリッジ（モバイルコンパニオン）** —— スマートフォンで QR コードをスキャンしてデスクトップとペアリングすると、**暗号化リレー**経由でデスクトップとの信頼性の高い双方向接続を確立し、デスクトップのタスクをスマートフォンからリモートで駆動できます：対話メッセージのリアルタイム送受信、**忠実な履歴リプレイ**（ユーザーメッセージ内のリッチテキスト / 画像はスマートフォン側に戻っても表示され、各 event にはグローバルに安定した連番が付きます）、タスク単位のルーティング、グレースフルな中断に対応し、入力ボックスも **/コマンド · @ファイル · 画像**などのリッチ入力をサポートします。タスクを開くと**権威ある状態スナップショット**とアーカイブマークを取得し、実行中 / アーカイブ済みが一目で分かります。デスクトップの **4 点セット設定（モデル / 推論強度 / 権限 / CLI）は双方向同期**——ローカルの変更はアプリへプッシュバックされ、リモートの変更はデスクトップに永続化されます。**OpenCode タスクのモデル / プロバイダー / 思考深度 / 権限レベルなどの設定も、スマートフォン側での表示・変更が可能**で、スラッシュコマンド / スキルのリストも OpenCode をカバーし、いずれも**タスクで選択された CLI に厳密に追従**します（下書き状態で CLI を切り替えると、コマンド / スキル / ワークフローのリストもそれに合わせて切り替わり、Claude 基準の固定リストではなくなりました）。インタラクティブな質問と権限リクエストは**一度回答すれば双方向に同期**され、デスクトップで回答すると回答済み状態がリアルタイムでスマートフォンへ伝わり、両端で二重に催促されることはありません。**未回答の権限リクエストはタスクを開き直しても引き続き回答できます**（停止 / 中断の経路も同期してクリーンアップされ、ゴースト承認カードは残りません）。**バックグラウンドタスクと動的ワークフローも同様にリモートから見えます**——スマートフォンはバックグラウンドタスクの状態をリアルタイムに受信し、バックグラウンドサブ Agent の内部対話はアクティブなターン中にリアルタイムでストリーミングされ、アプリのコールドスタート時にディスクから再構築されたワークフロー実行はタスクを開くと同時にバックフィルされ、ワークフローのサブエージェントの transcript もリモートで確認できます。基盤は**ネイティブ Rust** 実装です：ECDH ペアリング + エンドツーエンド暗号化、切断時は自動再接続・再送信でパケットを失わず、アプリケーション層ハートビートによるキープアライブと偽オンライン検出を備え、デバイスの連携解除は即時に反映され、解除された App へ明確な受領通知を返します。

### 対話体験
- **リッチテキストのメッセージバブル** —— Markdown レンダリング、表、コードのシンタックスハイライト、Markdown プレビュー。
- **ツール呼び出しのタイムラインカード** —— Read / Write / Edit の diff や WebSearch の結果などを連続したタイムラインカードに集約（連続する呼び出しは左端のラインを共有し、行ごとに展開して詳細を確認できます）。一目で把握でき、場所も取りません。
- **インタラクティブな質問（AskUserQuestion）** —— UI 上で選択肢を直接クリックでき、「その他」の自由入力にも対応しています。
- **AI メッセージツールバー**、対話内のリンクはシステムブラウザで開き、タスク状態と未読インジケーターも備えます。

### 入力（Composer）
- **スラッシュコマンドパレット**（`/command`）と **@ ファイルメンションパレット**、インラインのファイル chip。
- **画像入力**、**タスクごとの独立した下書き**、**モデル / 推論強度セレクター**（Claude ログインでは **Fable** / Opus / Sonnet / Haiku の各ティアを内蔵。Codex ログインでは **Codex OAuth** ネイティブティアと minimal～xhigh の推論強度を内蔵。OpenCode はアプリ内の **OpenAI 互換プロバイダー**を再利用し、タスクごとに思考深度を選択可能。モデルのドロップダウンは現在の CLI ファミリーでフィルタリングされます）、カスタマイズ可能な入力ツールバー。

### 統合開発ツール
- **統合ターミナル** —— xterm.js + PTY ベースの本物のターミナル。ターミナルログ内のリンクはクリックできます（左クリックで内蔵ブラウザで開き、右クリックでシステムブラウザを呼び出すかアドレスをコピー）。
- **ファイルツリーサイドバー + サイドファイルパネル + Monaco エディター** —— コードと Diff の閲覧・表示。
- **ファイルツリーの複数選択とファイル操作** —— クリックで選択 / ドラッグで矩形選択、コピー / 切り取り / 貼り付け（システムのファイルクリップボードと連携し、エクスプローラーと相互運用できます）、新規作成 / 名前変更 / 削除に対応し、`Del` と `Ctrl+C/X/V` のショートカットが使えます。
- **Git ワークフロー** —— ブランチ切り替え、変更 Review パネル、コミットメニュー（✨ ワンクリックのコミットメッセージ生成を含む）、worktree の自動検出と追従。
- **内蔵ブラウザパネル** —— 右側のパネルで Web ページを直接開けます。マルチタブ対応・タスク間共有で、ドキュメントの確認やページのプレビューにアプリを離れる必要はありません。
- 現在のワークスペースを**ファイルエクスプローラーで開く**。

### 設定と拡張
- **Skills（スキル）/ MCP サーバー / Plugins（プラグイン）/ サブエージェント（Subagents）** のビジュアル管理 —— **Claude / Codex / OpenCode** の 3 つの CLI ファミリーをカバーします（OpenCode は元々 Claude のスキルルートを共有しており、マネージドな `opencode.json` を通じて MCP と、スキル / サブエージェント / スラッシュコマンドの有効・無効を管理します）。サブエージェントは**ユーザー / プロジェクト / プラグイン**の 3 スコープにわたる表示・新規作成・編集・削除・有効 / 無効切り替えに対応します（プラグイン提供のものは読み取り専用）。
- **Model Providers（モデルプロバイダー）** の設定で、バックエンドを自由に切り替え —— 一般的なモデルには**コンテキストウィンドウの出荷時デフォルト値**が内蔵されており、モデルを追加するときは入力ボックスのプレースホルダーが推奨値を直接提示します（空欄のままでも妥当なデフォルトが使われます）。
- **依存関係管理 + ランタイムバージョン管理** —— 依存関係は必須 / オプションの層に分けて表示されます。**必須依存関係の Claude Code CLI と Git はアプリ内ワンクリックインストールに対応**（Windows では直接インストール、macOS の Git はシステムの Xcode コマンドラインツールへ誘導）し、**OpenAI Codex と OpenCode CLI もアプリ内でワンクリックインストール可能**です（3 つの AI CLI のうち少なくとも 1 つをインストールすれば十分です）。`uv` / `pnpm`（および `bun`）については複数バージョンの一覧表示 / インストール / アンインストール / 切り替えができ、ワンクリックのフォールバックインストールにも対応します。
- **プロキシ設定** —— http / https プロキシに対応します（SOCKS は非対応）。
- **環境変数設定** —— CLI サブプロセス（Claude / Codex）にカスタム環境変数を注入できます（変更は自動的にサブプロセスの再起動をトリガーします）。
- **使用量統計（Usage）** —— 消費状況を直感的に確認できます。

### デスクトップ体験
- **アプリ全体のカスタム右クリックメニュー** —— 対話エリア、プレビュー、ファイルツリー、タスク行、サイドバーの空白部分に、それぞれの場面に合った右クリックメニューを用意し、システム標準のメニューを置き換えます。
- **バックグラウンドのタスクバー通知** —— ウィンドウが前面にないとき（最小化を含む）、AI の完了 / 失敗 / 確認リクエストのポップアップがあると、QQ / WeChat のようにタスクバーアイコンが点滅します。
- **新規タスクは前回の設定を記憶** —— 新規タスク / スキルは、前回使用したモデルプロバイダーとパラメーターを自動的に引き継ぎます。
- **ネイティブウィンドウ装飾（macOS）** —— macOS ではシステムネイティブの Overlay タイトルバーと 3 色（赤・黄・緑）ボタンを採用し、サイドバーのヘッダーにはネイティブ 3 ボタン用のセーフエリアを確保、ノッチ付きディスプレイのフルスクリーンセーフエリアにも対応します。Windows はボーダーレスのカスタムウィンドウを維持します。
- **テーマと配色の一貫性（宣紙墨金）** —— まったく新しい「宣紙墨金」のビジュアル言語：丁寧にチューニングされた 3 つのテーマ（ダーク / ライト / 目に優しい黛青）、温かみのある宣紙の下地 + 墨色の文字 + アクセントの金印。コンテンツ見出しには Source Han Serif（源ノ明朝）のセリフ体を採用し、ダークテーマでは背景にほのかな金色のグローを添えています。対話エリア、入力エリア（紙面のフローティングボックス + 円形金印の送信ボタン）、タスク行（金のしおり型ステータススペクトラム）、設定ページ（紙面カードのグルーピング）、ファイルパネル / ターミナル / Monaco（gruvbox ANSI16）まで、アプリ全域で一貫した装いに統一。アプリ全体の配色は統一されたテーマ token の土台（ロール / カテゴリーカラーパレット / セマンティック token、ライト・ダーク自動適応）の上に構築されており、diff、サブ Agent、Review、使用量アラート、ボット接続インジケーター、ワークフロー状態、モデルカラーパレット、タグファミリーなどの場面はすべてハードコードされた色ではなく token を参照するため、テーマをまたいでも見た目が一貫し、デフォルトでシステムのライト / ダークに追従します。
- **新アプリアイコン「ウロボロスの環龍」** —— 玄墨と金の角丸方印バッジ：尾を銜えて環をなす小さな龍が玄墨の下地に鎮座し、金の鱗がアクセント。アプリ内サイドバーのロゴは 3 テーマ（ダーク / ライト / 目に優しい黛青）に追従して切り替わり、それぞれ専用の配色で表示され、目に優しいテーマのワードマークは黛青と同色を採用しています。
- **インターフェース言語（中国語 / 英語）** —— グラフィカルインターフェース全体が中国語と英語の切り替えに対応しています。デフォルトではシステム言語に追従し、判別できない場合は英語にフォールバックします。「設定 → 一般」からいつでも切り替えられます。AI の対話内容には影響しません。
- **中国語 / 英語のインストール画面** —— インストール時に言語を選択できます。
- **自動更新を内蔵** —— 起動後にサイレントにチェックし、アップグレードを促します。
- **初回ガイドツアー（Onboarding Tour）** —— 初めての利用でも迷いません。

---

## 💻 システム要件

| 項目 | 説明 |
| --- | --- |
| **オペレーティングシステム** | Windows 10/11（x64 / ARM64）、macOS（Apple Silicon および Intel、Universal） |
| **必須依存関係** | **Claude Code CLI**（インストール済みでログイン / 設定が完了していること）と **Git**。どちらもアプリ内でワンクリックインストール可能 |
| **オプション依存関係** | **OpenAI Codex CLI** —— Codex ファミリーのタスクを使いたい場合にインストール。同じくアプリ内ワンクリックインストールに対応 |
| **ネットワーク（オプション）** | プロキシを使う場合は http / https プロキシのみ対応（SOCKS は非対応） |

> 💡 **この 2 つの依存関係が未インストールでも問題ありません**：LoongCode をインストールした後、**設定 →「依存関係管理」** に進めば、Claude Code CLI と Git をワンクリックでインストールできます。コマンドラインを手で操作する必要はありません（詳細は下記「🧩 依存関係とワンクリックインストール」を参照）。
>
> 実際にダウンロードできるインストーラーについては、本リポジトリの [Releases](../../releases) ページの成果物を基準にしてください。

---

## 📥 ダウンロードとインストール

1. 本リポジトリの **[Releases](../../releases)** ページを開きます。
2. お使いのシステムに合ったインストーラーを選んでダウンロードします：
   - **Windows**：NSIS インストーラー（`.exe`）。インストールウィザードは中国語 / 英語のインターフェースを内蔵しています。
   - **macOS**：ディスクイメージ（`.dmg`）/ アプリバンドル。
3. インストーラーを実行し、ウィザードに従ってインストールを完了します。
4. 初回利用の前に、ローカル環境に **Claude Code CLI**（ログイン済み）と **Git** がインストールされていることを確認してください。まだの場合は、起動後に **設定 →「依存関係管理」** からワンクリックでインストールできます（下記「🧩 依存関係とワンクリックインストール」を参照）。

> システムが提供元に関するセキュリティ警告（Windows SmartScreen / macOS Gatekeeper）を表示した場合は、システムの案内に従ってそのまま実行を続けてください。

---

## 🧩 依存関係とワンクリックインストール

LoongCode の実行には、2 つの**必須依存関係**が必要です：

- **Claude Code CLI** —— モデル能力を提供します（アプリ自身が Anthropic API を直接呼び出すことはありません）。
- **Git** —— ブランチ切り替え、変更 Review、worktree などの Git ワークフローを駆動します。

まだインストールしていなくても大丈夫、**手でコマンドを打つ必要はありません**：LoongCode を開いて **設定 →「依存関係管理」** に進むと、「必須依存関係」グループにそれらのインストール状態が表示されます。**「インストール」** をクリックするだけでワンクリックでセットアップ完了です。

| 必須依存関係 | Windows | macOS |
| --- | --- | --- |
| **Claude Code CLI** | ワンクリックインストール（公式インストールスクリプト） | ワンクリックインストール（公式インストールスクリプト） |
| **Git** | ワンクリックインストール（ポータブル版 PortableGit をアプリディレクトリにインストール） | 「システムインストールを起動」をクリックしてシステムの Xcode コマンドラインツールのインストールを呼び出し。`brew install git` を自分で実行しても OK |

- インストールの過程はログがリアルタイムに表示され、完了後は自動的に状態を再検出します。
- プロキシ環境では、このパネルで依存関係ごとに http / https プロキシを設定してからインストールできます。
- 「オプション依存関係」の `uv` / `pnpm` / `bun` も、ワンクリックインストールと複数バージョンの切り替えに対応しています。
- **OpenAI Codex CLI のオプション導入** —— Codex ファミリーのタスクを使いたい場合、依存関係管理は **Codex CLI のワンクリックインストール**にも対応しています（公式インストールスクリプト / GitHub 直接接続 / ミラー加速の 3 モード）。設定のスキル / プラグイン / MCP / サブエージェント各パネルには、いずれも Codex の軸が用意されています。
- **Claude Code CLI のインストール元を選択可能** —— 公式インストールスクリプト / GitHub 直接接続 / GitHub ミラー加速（ミラーのプレフィックスはカスタマイズ可能）から選べ、一部のネットワーク環境で公式ソースに到達できない場合に対応します。
- **Git のインストール元を選択可能（Windows）** —— GitHub 直接接続 / ミラー加速（ミラーのプレフィックスはカスタマイズ可能）。ポータブル版 PortableGit なら、ネットワークが制限された環境でも問題なくインストールできます。

> ⚠️ アプリにできるのは CLI を**インストールする**ところまでです。**ログインはご自身で行う必要があります**：統合ターミナルで `claude` / `codex` を実行してプロンプトに従いログインするか、**設定 →「Model Providers」** でお使いのバックエンドを設定してください。

---

## 🚀 クイックスタート

1. **依存関係を準備する** —— Claude Code CLI と Git をインストールし、CLI にログインします（Codex ファミリーのタスクを使うなら Codex CLI も追加でインストール）。未インストールでも、先に LoongCode を開いて **設定 →「依存関係管理」** からワンクリックでインストールできます。
2. **LoongCode をインストールして開きます**。
3. **ワークスペースを追加する** —— プロジェクトのディレクトリを指定します。
4. **新規タスクを作成する** —— 要望を入力して Enter を押せばセッションが始まります（入力バーで Claude Code か OpenAI Codex を選択できます）。
5. 右側の**ターミナル、ファイルツリー、Git Review** で変更を追いかけ、必要に応じて設定から **MCP / プラグイン / スキル / モデルプロバイダー** を組み込みます。

---

## 🔄 自動更新

LoongCode は [Tauri Updater](https://v2.tauri.app/plugin/updater/) を内蔵しています。アプリは本リポジトリのリリースソースへ更新マニフェストを取得しに行きます：

```
https://github.com/LoongCode0/loongcode-release/releases/latest/download/latest.json
```

新しいバージョンを検出するとユーザーに通知します。Windows では passive（サイレント）インストールモードを採用しており、更新プロセスはほとんど意識せずに済みます。

---

## ❓ よくある質問

**Q：ローカルにまだ Claude Code CLI や Git がインストールされていません。手動でインストールする必要がありますか？**
A：いいえ。LoongCode を開いて **設定 →「依存関係管理」** に進み、「必須依存関係」で「インストール」をクリックするだけです。Windows では Claude Code CLI と Git のどちらも直接インストールできます。macOS では Git はシステムの Xcode コマンドラインツールのインストールが呼び出されます。Claude Code CLI をインストールしたら、忘れずにログインを済ませてください。

**Q：インストールしたのに対話できない / モデルの応答がまったくない？**
A：LoongCode 自体はモデル能力を持っておらず、ローカルの CLI ツールに依存しています。対応する CLI がインストール済みで、PATH に含まれ、ログインが完了していることを確認してください：Claude タスクには `claude`、Codex タスクには `codex` が必要です。

**Q：会社のネットワークでプロキシが必要な場合は？**
A：アプリの設定で http / https プロキシを設定してください。起動される CLI は http/https のみをサポートし、SOCKS には対応していない点にご注意ください。

**Q：更新チェックで新しいバージョンが見つからない？**
A：自動更新は本リポジトリ Releases の `latest.json` に依存しています。ネットワークから GitHub にアクセスできることを確認してください。

---

## 💖 応援・スポンサー

<div align="center">

LoongCode が役に立ったら、開発者にコーヒーを一杯おごって継続的な開発を応援してください ❤️

| WeChat Pay | Alipay |
| :---: | :---: |
| <img src="docs/assets/wx.png" width="200" alt="WeChat 応援用 QR コード"> | <img src="docs/assets/zfb.png" width="200" alt="Alipay 応援用 QR コード"> |

</div>

---
